<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
    <CentralPackageFloatingVersionsEnabled>true</CentralPackageFloatingVersionsEnabled>
  </PropertyGroup>

  <ItemGroup>
    <PackageVersion Include="Microsoft.AspNetCore.OpenApi" Version="10.*" />
    <PackageVersion Include="Microsoft.AspNetCore.Authentication.Google" Version="10.*" />
    <PackageVersion Include="Scalar.AspNetCore" Version="2.*" />

    <!-- OpenTelemetry.Api pinned to avoid GHSA-8785-wc3w-h8q6 / GHSA-g94r-2vxg-569j (1.10.0). -->
    <PackageVersion Include="OpenTelemetry" Version="1.15.*" />
    <PackageVersion Include="OpenTelemetry.Api" Version="1.15.*" />
    <PackageVersion Include="OpenTelemetry.Extensions.Hosting" Version="1.15.*" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.AspNetCore" Version="1.15.*" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.Http" Version="1.15.*" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.Runtime" Version="1.15.*" />
    <PackageVersion Include="OpenTelemetry.Instrumentation.Process" Version="1.15.*-*" />
    <PackageVersion Include="OpenTelemetry.Exporter.Prometheus.AspNetCore" Version="1.15.*-*" />
    <PackageVersion Include="OpenTelemetry.Exporter.Console" Version="1.15.*" />

    <PackageVersion Include="xunit.v3.mtp-v2" Version="3.*" />
    <PackageVersion Include="Microsoft.AspNetCore.Mvc.Testing" Version="10.*" />
    <PackageVersion Include="Shouldly" Version="4.*" />
    <PackageVersion Include="Testcontainers.PostgreSql" Version="4.*" />

    <!-- EF Core + Postgres (PORTAL-002, ADR-0028/0029) -->
    <PackageVersion Include="Microsoft.EntityFrameworkCore" Version="10.*" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Design" Version="10.*" />
    <PackageVersion Include="Microsoft.EntityFrameworkCore.Relational" Version="10.*" />
    <PackageVersion Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.*" />
    <PackageVersion Include="Npgsql.EntityFrameworkCore.PostgreSQL.NodaTime" Version="10.*" />
    <PackageVersion Include="EFCore.NamingConventions" Version="10.*" />
    <PackageVersion Include="Microsoft.Extensions.Diagnostics.HealthChecks.EntityFrameworkCore" Version="10.*" />

    <!-- Time -->
    <PackageVersion Include="NodaTime" Version="3.*" />
    <PackageVersion Include="NodaTime.Serialization.SystemTextJson" Version="1.*" />

    <!-- Auth / TOTP (PORTAL-003a) -->
    <PackageVersion Include="Otp.NET" Version="1.4.*" />
    <PackageVersion Include="QRCoder" Version="1.6.*" />
    <PackageVersion Include="Konscious.Security.Cryptography.Argon2" Version="1.3.*" />

    <!-- FIDO2/WebAuthn passkeys (PORTAL-PASSKEY-AUTH). Fido2.AspNet brings Fido2 + Fido2.Models. -->
    <PackageVersion Include="Fido2" Version="4.*" />
    <PackageVersion Include="Fido2.AspNet" Version="4.*" />

    <!-- Test isolation -->
    <PackageVersion Include="Respawn" Version="6.*" />
    <PackageVersion Include="NodaTime.Testing" Version="3.*" />
    <PackageVersion Include="Microsoft.Extensions.TimeProvider.Testing" Version="9.*" />

    <!-- Transitive pins to satisfy CVE warnings-as-errors (Respawn 6 pulls SqlClient which transitively pulls these). -->
    <PackageVersion Include="Azure.Identity" Version="1.13.*" />
    <PackageVersion Include="Microsoft.IdentityModel.JsonWebTokens" Version="8.*" />
    <PackageVersion Include="System.IdentityModel.Tokens.Jwt" Version="8.*" />
    <PackageVersion Include="System.Drawing.Common" Version="8.*" />

    <!-- Cloud-init template-render tests (PORTAL-010). -->
    <PackageVersion Include="YamlDotNet" Version="16.*" />

    <!-- Polly + IHttpClientFactory (PORTAL-010b). -->
    <PackageVersion Include="Microsoft.Extensions.Http.Polly" Version="9.*" />

    <!-- WireMock for CloudflareDnsClient tests (PORTAL-010b). -->
    <PackageVersion Include="WireMock.Net" Version="1.7.*" />

    <!-- S3-compatible storage (DO Spaces, R2, MinIO) for IArtifactStore (CLOUD-002). -->
    <PackageVersion Include="AWSSDK.S3" Version="4.*" />
    <PackageVersion Include="Testcontainers.Minio" Version="4.*" />

<!-- LLM (CLOUD-002 Pass B): Microsoft.Extensions.AI.IChatClient abstraction +
         LlmTornado multi-provider adapter (30+ native providers including Anthropic).
         Provider swap is a ChatModel argument change; no parallel client per vendor. -->
    <PackageVersion Include="Microsoft.Extensions.AI" Version="10.*" />
    <PackageVersion Include="LlmTornado" Version="3.*" />
    <PackageVersion Include="LlmTornado.Microsoft.Extensions.AI" Version="1.*" />

    <!-- pgvector EF mapping (CLOUD-SCHEMA-V2). -->
    <PackageVersion Include="Pgvector" Version="0.3.*" />
    <PackageVersion Include="Pgvector.EntityFrameworkCore" Version="0.3.*" />

    <!-- Image decode + EXIF + pHash for VLM pre-flight (CLOUD-PROCESSORS-LIGHT). -->
    <PackageVersion Include="SixLabors.ImageSharp" Version="3.*" />
    <PackageVersion Include="CoenM.ImageSharp.ImageHash" Version="1.*" />

    <!-- PDF page-count + size preflight for Docling (CLOUD-PROCESSORS-HEAVY Pass A). -->
    <PackageVersion Include="PdfPig" Version="0.1.*" />

    <!-- WAV decoding for silence-VAD audio preflight (CLOUD-PROCESSORS-HEAVY Pass B). -->
    <PackageVersion Include="NAudio.Core" Version="2.2.*" />

    <!-- Embedding (CLOUD-EMBEDDING handoff #8): in-process Granite Embedding R2 ONNX.
         Tokenizers.DotNet wraps HF tokenizers (Rust) and loads tokenizer.json directly;
         Microsoft.ML.Tokenizers 1.0.x lacks an equivalent CreateFromConfig API. -->
    <PackageVersion Include="Microsoft.ML.OnnxRuntime" Version="1.20.*" />
    <PackageVersion Include="Tokenizers.DotNet" Version="1.4.*" />
    <PackageVersion Include="Tokenizers.DotNet.runtime.osx-arm64" Version="1.4.*" />
    <PackageVersion Include="Tokenizers.DotNet.runtime.linux-x64" Version="1.4.*" />
  </ItemGroup>
</Project>
